# Repository Instructions

Build a provider-neutral connection service. Preserve tenant isolation, default-deny permissions, opaque credential handles, explicit errors, human approval for increased risk, and auditable connector lifecycle transitions. Never commit secrets or production customer data. Keep MCP, REST/OpenAPI, databases and browser automation as replaceable adapters behind one ConnectorContract.
